name: build

on:
  push:
    branches: [ "*" ]
  pull_request:
    branches: [ master ]

# ----------------------------------------------------------------------------
# Lint code and run unit tests.
#
# On Linux, also start KinD cluster, run the integration tests and upload the
# coverage to CodeCov.
# ----------------------------------------------------------------------------
jobs:
  tests:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6

      - name: Start KinD Cluster
        if: matrix.os == 'ubuntu-latest'
        run: |
          mkdir ~/bin
          curl -L --output ~/bin/kubectl https://dl.k8s.io/release/v1.34.2/bin/linux/amd64/kubectl
          curl -L --output ~/bin/kind https://github.com/kubernetes-sigs/kind/releases/download/v0.30.0/kind-linux-amd64
          chmod +x ~/bin/kubectl ~/bin/kind

          cd ./integration-test-cluster
          ./start_cluster.sh
          cd ..

      - uses: actions/setup-python@v6
        with:
          python-version: '3.14'

      - name: Install UV and Square Dependencies
        run: |
          pip install uv

      - name: Install Extra Dependencies (Windows only)
        if: matrix.os == 'windows-latest'
        run: |
          uv add macholib pypiwin32 atomicwrites

      - name: Linting and Static Analysis
        run: |
          uv sync
          uv tool run ruff check
          uv tool run ruff format --check
          uv tool run ty check

      - name: Unit Tests
        if: matrix.os != 'ubuntu-latest'
        run: |
          uv run pytest --cov=square --ignore=tests/test_integration.py --durations=10

      # Break if coverage drops below 100%.
      - name: Unit and Integration Tests (Linux only)
        if: matrix.os == 'ubuntu-latest'
        run: |
          uv run pytest --cov=square --cov-report=term-missing --cov-report=xml --cov-fail-under=100 --durations=10

      - uses: codecov/codecov-action@v5
        if: matrix.os == 'ubuntu-latest'
        with:
          files: ./coverage.xml
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: true
          verbose: false

  # ----------------------------------------------------------------------------
  # Build and install Square on recent Python versions.
  # ----------------------------------------------------------------------------
  examples:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.13", "3.14"]
    steps:
      - uses: actions/checkout@v6

      - name: Start KinD Cluster
        run: |
          mkdir ~/bin
          curl -L --output ~/bin/kubectl https://dl.k8s.io/release/v1.34.2/bin/linux/amd64/kubectl
          curl -L --output ~/bin/kind https://github.com/kubernetes-sigs/kind/releases/download/v0.30.0/kind-linux-amd64
          chmod +x ~/bin/kubectl ~/bin/kind

          cd ./integration-test-cluster
          ./start_cluster.sh
          cd ..

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install Square
        run: |
          # Build the Python package.
          pip install uv
          uv build

          # Install the package. The following shell magic expands to
          # eg `pip install dist/kubernetes-square-1.3.2.tar.gz`
          pip install "dist/kubernetes_square-`uv version --short`.tar.gz"

          # Invoke Square to ensure it runs.
          square version

      - name: Run Examples
        working-directory: ./examples
        run: |
          python basic_workflow.py
          python custom_callbacks.py
          python multi_cluster.py

  build-docker-image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: docker/build-push-action@v4
        with:
          context: .
          load: true
          tags: latest
      - name: Run Square
        run: |
          docker run --rm latest

  build-binaries:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: '3.14'

      - name: Install UV
        run: |
          pip install uv

      - name: Install Extra Dependencies (Windows only)
        if: matrix.os == 'windows-latest'
        run: |
          uv add macholib pypiwin32 atomicwrites

      - name: Build Binary
        run: |
          uv add pyinstaller==6.19.0 --dev
          uv run pyinstaller square.spec --clean

          # Verify that the binary runs.
          dist/square version
